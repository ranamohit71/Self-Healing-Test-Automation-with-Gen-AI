1. Open Command prompt and create a virtual env: python -m myproject venv
2. Activate the environment: myproject\Scripts\activate
3. Install requirements from txt file: pip install -r .\requirements.txt
4. verify packages: pip list
5. Run test: pytest tests\ -s -v
